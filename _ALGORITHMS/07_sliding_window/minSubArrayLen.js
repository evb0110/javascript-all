function minSubarrayLen(arr, num) {
  if (!arr.length) return null;
  const min = +Infinity;
  const minLength = 0;
  const start = 0;
  const end = 0;
  const sum = arr[start];
  while (start < arr.length - 1) {
    if (sum > num) {

    }
  }
}